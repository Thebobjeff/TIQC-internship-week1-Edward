How to Run:

Install dependencies: npm install

Run via ts-node: npx ts-node src/index.ts list

(Optional) Link for global use: npm link then use todo-cli list

Assumptions:

Tasks are stored in the same directory as the script.

Unique IDs are generated using Date.now().
